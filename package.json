{
  "name": "rainbox-six-7",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "RAINBOX SIX 7 — a self-contained ASCII terminal tactics game in vanilla JavaScript (no runtime dependencies).",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "node scripts/build.js",
    "preview": "node scripts/preview.js",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "terser": "^5.44.0"
  }
}
